# 更新日志

## Unreleased

- 新增 `!add/remove share <群号...> <联动组名>`，管理员可创建命名联动组并跨群调整成员。
- 联动组成员只共享彼此的本群词库，不继承成员群的全局词库、外部导入词库或其他联动组权限。
- `!grouplist` 私聊结果增加完整联动组，群聊结果只显示当前群加入的组名，不披露其他成员群号。

## 0.2.0-beta - 2026-08-15

- 将公开 Beta 版本统一命名为 `0.2.0-beta`，同步更新插件元数据、运行版本、安装说明和 Release Notes 文件名。
- 修复 GitHub 源码包中 `metadata.yaml`、更新日志和内嵌 WebUI 页面因编码转换出现问号的问题。
- WebUI 保持紧凑控制台、无密码进入、一小时管理会话和高风险操作二次确认。

## 0.1.0-beta.31.post22 - 2026-08-15

- WebUI 升级为面向群聊机器人管理员的紧凑控制台，使用固定侧栏、动态页面标题和清晰的运行状态层级。
- 概览、表单、表格、列表、状态提示与确认弹窗统一视觉和交互规范，并保留无密码进入与一小时管理会话。
- 新增手机尺寸的可关闭导航侧栏，切换模块后自动收起；长表单和表格不再造成整页横向溢出。
- 补充键盘焦点、活动导航、减少动态效果和禁用/忙碌状态的界面契约检查。

## 0.1.0-beta.31.post21 - 2026-08-15

- 旧 `.cl` 导入改为独立外部词库，不再与群聊自产问答混合。
- 外部词库支持按群绑定、单独启用或停用，以及保留词库 ID 的完整替换更新。
- 删除外部词库前自动备份，只移除对应外部来源；历史混合导入不会被推测拆分。
- WebUI 迁移页新增外部词库状态、版本、绑定群和生命周期操作。

## 0.1.0-beta.31.post20 - 2026-08-15

- 群聊中的跨群管理写入回执只显示操作结果和目标数量，不再附带完整群配置。
- 群聊中的 `!grouplist` 只展示当前群状态；管理员私聊仍保留完整群列表。
- 目标群资料读取失败时，群聊回执不再显示目标群号。

## 0.1.0-beta.31.post19 - 2026-08-15

- 新增 `!reply -s <百分比> <群号...>`，可从管理员私聊或任意管理群设置目标群独立回复概率。
- 新增 `!reply -d <群号...>`，恢复目标群继承全局回复概率。
- `!grouplist`、WebUI 群聊设置和实际回复决策现在共享同一份按群概率配置。

## 0.1.0-beta.31.post18 - 2026-08-15

- 修复更新日志存在重复标题，导致插件管理页面只显示到 post13 的问题。
- 合并 post14 至 post17 的版本记录并恢复按版本倒序展示。

## 0.1.0-beta.31.post17 - 2026-08-15

- 修复原始 OneBot 消息补全时文本与 QQ 表情组件被重复追加，导致 Bot 重复发送文本内容的问题。
- 组件合并改为按类型和数量补齐，只保留 AstrBot 未解析出的原始组件。

## 0.1.0-beta.31.post16 - 2026-08-15

- 补强图片、视频、语音、文件、商城表情、JSON、XML 和合并转发消息的原始 OneBot 段补全。
- AstrBot 预处理丢失媒体组件时，改用原始消息段继续学习与匹配。
- NapCat 媒体查询改为本地文件优先，临时 URL 仅作为最后兜底，降低 QQ 图片链接过期造成的词条失效。

## 0.1.0-beta.31.post15 - 2026-08-15

- 修复图片、XML 等原始 OneBot 回复组件缺少 AstrBot 类型属性，导致本地消息历史写入失败的问题。

## 0.1.0-beta.31.post14 - 2026-08-15

- 修复失效媒体引用在 NapCat 发送阶段失败时导致回复处理异常的问题。
- 混合文本与媒体的回复会在媒体不可用时保留文本并重试；纯失效媒体回复会安全跳过。

## 0.1.0-beta.31.post13 - 2026-08-15

- `/ncl help` 与私聊 `!help` 现在分别列出 `!add globe`、`!remove globe` 和 `!add/remove unmerge` 的完整用法与作用方向。
- 跨群帮助明确区分目标群查询范围与来源群汇入范围。

## 0.1.0-beta.31.post12 - 2026-08-15

- 新增 `!add/remove globe <群号...>`，管理员可按目标群控制是否查询全局或标签共享词库；使用该命令后改由显式群列表取代旧的全部群模式。
- `unmerge` 继续只控制来源群是否汇入全局词库，两个方向互不混用。
- `!grouplist` 增加使用全局词库和仅使用本群词库的群列表，并兼容旧全局模式配置。

## 0.1.0-beta.31.post11 - 2026-08-15

- 修复图片、语音和视频消息同时包含文件标识与临时 URL 时，错误优先下载临时 URL 的问题。
- 优先使用 OneBot 原始媒体段交给 NapCat 重发，避免过期 URL 触发 AstrBot 媒体下载异常。
- 适配媒体解析器抛出的平台特定异常，避免失效媒体中断整条回复处理。

## 0.1.0-beta.31.post10 - 2026-08-15

- 修复词库中的失效图片文件标识被误当作本地路径，导致回复处理抛出文件不存在错误的问题。
- 媒体无法恢复时，文本回复仍可正常发送；纯媒体且无法恢复时会安全跳过。

## 0.1.0-beta.31.post9 - 2026-08-15

- 修复图片临时 URL 下载失败时媒体无法长期保存的问题，问答采集仍会保留原始图片组件。
- 新增 NapCat `get_image` / `get_file` 回退，优先使用文件标识解析本地媒体，再执行本地持久化与 SHA-256 去重。

## 0.1.0-beta.31.post8 - 2026-08-14

- 修复 AstrBot 配置对象包含线程锁时，跨群 `!add/!remove` 命令因 `deepcopy` 失败而无法保存的问题。
- 配置回滚现在只复制配置数据，不复制 AstrBot 配置包装对象本身。

## 0.1.0-beta.31.post7 - 2026-08-14

- 修复管理员私聊 `!help`、全角 `！help` 和 `/ncl help` 无响应的问题。
- 普通成员发送这些管理帮助命令仍保持静默。

## 0.1.0-beta.31.post6 - 2026-08-14

- 修复 AstrBot 4.27.2 中私聊事件过滤器使用 `PRIVATE_MESSAGE` 命名，而错误引用 `FRIEND_MESSAGE` 导致插件导入失败的问题。
- 已使用 AstrBot 实际 `.venv` 完成插件入口模块导入验证。

## 0.1.0-beta.31.post5 - 2026-08-14

- 恢复原版 `!grouplist` 与 `!add/!remove learning|learnings|reply|tag|subadmin|unmerge` 跨群控制语义。
- 按消息场景恢复 `!learning`/`!reply`：管理员私聊控制全局主开关，群聊控制当前群能力。
- 跨群命令仅允许 AstrBot 全局管理员或插件管理员执行，配置整批原子保存并写入审计。
- `!add subadmin` 通过 NapCat/AstrBot 读取目标群当前群主和群管理员名单；读取失败时不保存部分修改。

## 0.1.0-beta.31.post4 - 2026-08-14

首个公开预览版本，基于 Beta 31 云端 TTS 能力和 Beta 31.post2 的 WebUI 会话基础。

### 主要内容

- AstrBot + NapCat + OneBot v11 + QQ 群的 ChatLearning 兼容核心。
- 相邻消息学习、精确/正则/本地相似度匹配、权重和回复策略。
- 分群词库、静默学习、定向学习、过滤、黑名单和成员贡献清理。
- 图片、语音、视频、文件、商城表情、XML、转发等消息组件的本地化与降级处理。
- 旧 `.cl` 词库隔离扫描、群绑定导入计划、自动备份和统计核对。
- WebUI 概览、群设置、词库、媒体、过滤、权限、迁移、TTS、任务、备份、审计和诊断页面。
- Windows 本地 TTS、GPT-SoVITS、通用本地 HTTP TTS，以及可选云端 TTS 驱动。

### WebUI 与热更新

- WebUI 使用无密码“进入”入口，不保存旧版独立管理密码。
- 进入后建立一小时服务端会话，写操作继续使用 CSRF，高风险操作需要二次确认。
- 启动时清理旧 `webui-password.json`。
- 修复 AstrBot 热更新后顶层 `new_chat_learning.*` 模块缓存残留导致前后端版本错配的问题。
- 页面检测到前后端入口模式不一致时停止请求并提示重新安装。

### 已知限制

- 当前仅支持 Windows + AstrBot + NapCat + QQ 群。
- 完整 NapCat 消息类型、长时间任务、大规模词库和全部 WebUI 页面仍需社区实机验证。
- 词库媒体问答人工编辑与批量操作暂不纳入当前产品范围。
- 云端 TTS 已实现配置、DPAPI 密钥保护和额度控制，但真实付费服务商调用需要用户自行验收。

### 校验

安装包：`NewChatLearning-0.1.0-beta.31.post4.zip`

SHA-256：`3FC9A9150DA0EE1E0088CB970473909B4A83DF728CCFAA7D35BBDCB5C554E9C3`
