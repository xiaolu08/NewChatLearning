---
title: NewChatLearning 首个 Beta 验收标准
version: 0.5
date: 2026-08-13
status: 当前有效
---

# NewChatLearning 首个 Beta 验收标准

## 发布要求

首个可运行 Beta 只有在原版功能、现代增强、安全与恢复四个方面全部通过后才算完成。单元测试通过但未在 Windows + AstrBot + NapCat + QQ 群实测的项目不能发布为首个 Beta。

## 验收清单

### 原版兼容

- [ ] 相邻消息问答链、超时重开链、同一成员连续消息。
- [ ] 学习/回复总开关和群列表。
- [x] 分群、实时全局联合、排除全局、群标签（离线测试通过，仍待实机与大词库验收）。
- [x] 精确、正则、jieba 余弦匹配及阈值（离线测试通过，仍待实机验收）。
- [x] 答案重复权重随机、类型阈值、概率、等待、冷却、长度限制（离线测试通过，仍待实机验收）。
- [ ] @触发、文本占位格式化和消息链回复。
- [ ] 过滤、敏感词、黑名单容错、自动清理（回复过滤、敏感计数、阈值拉黑、全局/分群作用域、人工解封和管理员主动过滤清理已离线测试；无人值守定时清理及实机验收未完成）。
- [ ] 管理模式：搜索、查看、删除、自定义问答和权重（文本/正则短命令已离线测试；媒体问答、批量管理和实机验收待完成）。
- [x] 快速删除与回复记录（离线测试通过，仍待 NapCat 实机验收发送回执、引用和撤回）。
- [ ] 定时任务与原版特殊发送动作。
- [ ] 语音回复、概率、音色/模型选择。
- [ ] 导入词库、统计检查、Excel 导出（旧词库受限导入与当前群 ZIP/XLSX/JSONL 导出已离线验证；旧 `.cl` 反向导出、真实 Excel 与实机验收待完成）。
- [ ] 原版命令别名与帮助行为。

### 消息兼容

- [ ] 文本、图片、闪照、普通/商城表情、@、引用、语音（核心类型已离线测试，商城表情和实机验收未完成）。
- [ ] 转发、App、XML、JSON、文件、音乐分享、骰子、视频。
- [ ] 媒体下载、哈希去重、重发、过期降级和配额超限（核心路径、只读健康扫描和两阶段清理已离线测试；主动远程探测和实机验收未完成）。
- [x] 三份共享 `.cl` 均可扫描并生成准确报告（隔离扫描与离线样本已验证；WebUI 上传仍待 AstrBot 实机验收）。
- [x] 至少一份共享词库可受限导入；失效媒体不导致崩溃（2.1 MB 共享词库已完成临时库导入，数据库与备份完整性为 `ok`；旧媒体有效性报告待继续完善）。

### 新增能力

- [ ] 静默学习群绝不自动词库回复，管理员命令仍可用（离线配置与回复路径已验证，待 NapCat 实机验收）。
- [ ] 定向用户学习只采集目标用户的紧随答案（离线相邻消息链、非目标跳过与目标固化已验证，待 NapCat 实机验收）。
- [ ] 自身回复与管理命令不学习；其他 QQ Bot 消息可学习。
- [ ] 撤回尚未固化消息可修复学习链。
- [x] 按成员删除贡献并自动备份（按群预览、共享答案减权、独占答案删除、待固化消息清理、执行前备份和审计已离线验证；实机验收待完成）。
- [ ] 命中发送后停止当次 LLM；下一次 LLM 可看到词库回复或明确报告兼容关闭原因。
- [ ] 普通成员插件命令全程静默（快速删除入口已覆盖，完整命令集待实现）。

### WebUI 与安全

- [ ] 首次强制设密、Argon2id/scrypt、登录限流、锁定、退出和改密（scrypt 路径已离线测试；AstrBot 实机和 Argon2id 可选路径待完成）。
- [ ] CSRF、会话 Cookie、无缓存、安全响应头和高风险二次确认（认证基础与现有高风险操作已离线测试；AstrBot 实机验收待完成）。
- [ ] 默认本机访问，局域网模式需显式开启。
- [ ] 概览、群目标、词库、媒体、过滤、权限、TTS、迁移、任务、备份、审计页面可用（概览、群聊与定向目标、文本/正则词库、媒体清理、过滤规则测试、黑名单、权限、本地 TTS、旧词库迁移、备份恢复和审计页已实现；桌面/移动离线验收持续执行）。
- [ ] WebUI、AstrBot 配置和命令同步，无静默覆盖冲突。
- [ ] DPAPI 密钥加密、掩码显示、日志和备份不泄密。

### TTS

- [x] Windows 本地、GPT-SoVITS、通用本地 HTTP（离线测试通过；Windows `System.Speech` 已完成本机真实 WAV 冒烟测试，NapCat 语音发送待实机验收）。
- [ ] 火山、阿里、腾讯、Azure、OpenAI、OpenAI 风格和自定义 HTTP 映射。
- [ ] 云端默认关闭、费用警示、频率/每日额度、超时和错误脱敏。

### 可靠性

- [ ] 10 万问题/30 万答案级导入和查询基准满足可接受响应时间。
- [ ] AstrBot 重载、插件重载、NapCat 断线重连不损坏学习链或数据库。
- [ ] SQLite 事务、迁移、备份校验、恢复预览和回滚可用（备份浏览、只读校验、旧 schema 迁移、恢复前保险备份、原子替换和失败回滚已离线验证；实机重载验收待完成）。
- [ ] 媒体达到 10 GB 配额时文本学习继续，旧媒体不自动丢失。
- [ ] 所有高风险操作有审计记录。

当前媒体流程已验证本地健康文件、缺失文件、路径越界、哈希不一致、旧远程 URL、混合答案影响计算、扫描不删除答案、默认裁剪、显式整条删除、空问题清理、重复答案合并、操作者绑定、陈旧计划拒绝、备份完整性和审计。主动远程可用性探测与 NapCat 实机验收仍未完成，因此不能视为媒体生命周期验收完成。

当前词库 WebUI 已离线验证登录隔离、CSRF 写保护、正则输入校验、群作用域、删除前二次确认、备份完整性、审计保留与绝对路径脱敏。媒体问答编辑、批量操作和 AstrBot/NapCat 实机验收仍未完成。

当前群聊设置已离线验证五种群模式、定向用户归一化、AstrBot 配置对象异步持久化、保存失败回滚、revision 冲突拒绝、CSRF 和配置审计。真实 AstrBot Dashboard 与插件配置文件的双入口交替保存仍待实机验收，因此配置同步条目尚未勾选完成。

Beta 25 新增的群管理命令已离线验证标准命令状态转换、目标用户增删、普通成员静默、兼容别名开关、命令消息不进入学习链、保存失败提示和 `command` / `legacy_command` 审计来源。`!grouplist` 只显示当前群状态，不暴露其他群设置。AstrBot 命令分发、NapCat 群事件优先级和真实配置热保存仍待 Windows 实机验收。

当前过滤流程已离线验证普通规则不阻止学习、敏感词计数、阈值消息不学习、封禁后持续排除、全局/分群隔离、候选在权重抽样前剔除、非法/超时正则容错、配置保存回滚与冲突、CSRF、无敏感正文命中日志以及人工封禁/解封审计。AstrBot/NapCat 实机消息组件和大词库规则性能仍待验收。

当前过滤词库清理已离线验证无正文预览、群与操作者绑定、一小时计划、配置 revision 变化拒绝、答案变化拒绝、执行前完整备份、单事务删除、回复追踪失效、空问题清理、最小化审计、CSRF、二次确认和备份路径脱敏。该能力只允许管理员主动执行；无人值守定时删除仍未实现。

当前备份恢复已离线验证仅列出插件备份目录的 SQLite 文件、路径穿越拒绝、只读 `quick_check`、表计数、未来 schema 拒绝、旧 schema 自动迁移、恢复前保险备份、WAL/SHM 清理、原子替换、恢复审计、失败时运行库不变、绝对路径脱敏、CSRF、二次确认以及恢复后全部 WebUI 会话失效。Windows + AstrBot 运行时重载与大数据库恢复耗时仍待实机验收。

当前成员贡献清理已离线验证计划绑定群、成员和操作者，一小时过期，预览不包含聊天正文；执行时重新检查贡献与答案权重，数据变化则拒绝。共享答案仅扣除该成员的贡献次数，权重归零的独占答案才删除，并同步失效回复追踪、清理空问题和该成员待固化消息。旧词库导入与管理员自定义问答没有成员归属，不会被推测删除。执行前创建完整数据库备份，WebUI 要求 CSRF 与二次确认且只返回备份文件名。AstrBot/NapCat 实机身份与高并发学习场景仍待验收。

当前审计页已离线验证登录保护、按动作筛选、按 ID 倒序游标分页和 1 至 100 条分页限制。服务端只从明确白名单生成摘要，隐藏客户端 IP、WebUI 会话摘要、绝对或相对路径、聊天正文、密码及未知字段；前端使用文本节点渲染。审计覆盖完整性仍需随未实现的云端 TTS、任务等高风险功能继续扩展，AstrBot Dashboard 桌面与移动端显示待实机验收。

当前权限页已离线验证登录保护、一小时会话、CSRF、明确二次确认、QQ 号与数量限制、去重、AstrBot 配置持久化、保存失败回滚、revision 冲突拒绝和最小化审计。AstrBot 全局管理员仍由平台判定并始终优先；真实 Dashboard 与配置页交替修改仍待实机验收。

当前本地 TTS 已离线验证默认关闭、概率与文本长度限制、仅纯文本答案转换、混合媒体跳过、合成失败回退原文本、AstrBot 历史写入原文本、Windows 真实 WAV 生成、环回 URL 限制、禁用代理和重定向、10 MB 响应限制、音频签名检查、临时文件清理、配置回滚与 revision 冲突。GPT-SoVITS 和通用 HTTP 仍需连接真实本地服务验收；NapCat 语音发送、音色枚举和付费云端驱动仍待完成。

Beta 26 当前群词库导出已离线验证 SQLite 一致性快照、空词库、问题与答案计数、完整组件 JSONL、XLSX ZIP/XML 结构、公式注入防护、临时文件清理、审计失败不留文件、WebUI 登录与 CSRF、明确二次确认、五分钟一次性票据、会话绑定和重复下载拒绝。LibreOffice 未安装，因此尚未完成真实 Microsoft Excel/LibreOffice 打开验证；AstrBot Dashboard 文件下载与 NapCat 环境也仍待 Windows 实机验收。

Beta 27 旧词库迁移页已离线验证 `.cl` 扩展名与 128 MB 上限、短期上传授权、随机临时文件、实际大小校验、隔离安全扫描、无正文兼容摘要、会话绑定上传记录、会话与目标群绑定的一小时转换计划、过期清理、二次确认、导入前备份和绝对路径脱敏。既有 2.1 MB 真实共享词库已在临时 SQLite 中完成完整导入；AstrBot Dashboard 的原生文件上传、Windows 长耗时大文件表现和 NapCat 环境中的目标群核对仍待实机验收。

### 发布

- [ ] README、metadata、版本和仓库描述均显示 Beta / 非正式。
- [ ] AGPL-3.0、NOTICE、原作者归属和修改说明完整。
- [ ] 安装、配置、迁移、隐私、备份、恢复和故障排查文档完整。
- [x] GitHub 私有仓库 `xiaolu08/NewChatLearning` 已建立并完成首次推送。

## 相关文档

- [产品需求与约束](../product/requirements.md)
- [系统设计](../architecture/system-design.md)
- [NapCat 消息兼容矩阵](../compatibility/napcat-messages.md)
- [WebUI 与安全规范](../security/webui-security.md)

## 变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-08-13 | 0.5 | 增加旧 `.cl` WebUI 受限上传、隔离扫描、会话/群绑定计划和二次确认导入的离线验收记录。 |
| 2026-08-13 | 0.4 | 增加当前群 ZIP/XLSX/JSONL 导出、安全下载票据和公式注入防护的离线验收记录。 |
| 2026-08-13 | 0.3 | 增加群运行模式、定向学习命令和原版兼容别名的离线验收记录。 |
| 2026-08-13 | 0.1 | 建立首个全功能 Beta 的发布门槛。 |
| 2026-08-13 | 0.2 | 完成三种本地 TTS 驱动、语音管理页与 Windows 本机真实合成验证。 |
