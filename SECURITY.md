# 安全策略

## 支持范围

当前公开预览版本为 `0.1.0-beta.31.post4`。Beta 版本仅支持 Windows + AstrBot 4.27.2+ + NapCat + OneBot v11 + QQ 群聊。

安全问题请不要直接提交到公开 Issue。请使用 GitHub 的 **Security Advisories / 私密漏洞报告**，并尽量提供：

- 受影响的版本号和运行环境
- 最小复现步骤
- 影响范围和可能的利用条件
- 脱敏后的日志或截图

请不要上传 QQ 聊天正文、`.cl` 词库、数据库、媒体文件、备份、Cookie、API Key 或其他凭据。

## 数据安全提醒

- 旧 `.cl` 文件必须通过插件迁移页面的隔离扫描，不要在主进程中直接反序列化未知 pickle 文件。
- 云端 TTS 默认关闭；启用后可能产生第三方服务费用。
- 群聊词库可能包含个人信息和敏感内容，请在分享诊断信息前完成脱敏。
- WebUI 高风险操作会要求二次确认，并继续校验会话、CSRF、备份和审计条件。

## 支持版本

Beta 阶段只维护最新公开预览版本。旧版本如果存在安全问题，建议先升级到最新 Beta，再提交复现信息。
